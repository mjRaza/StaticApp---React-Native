import React, { Component } from "react";
import { Text, View } from "react-native";

export class Dos extends Component {
  static navigationOptions = {
    title: "Do's & Don't in Malaysia"
  };
  render() {
    return (
      <View>
        <Text> textInComponent </Text>
      </View>
    );
  }
}
export default Dos;
